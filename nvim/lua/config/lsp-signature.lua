require('lsp_signature').setup {
    floating_window = true,
    hint_enable = false,
    hint_prefix = '',
    max_height = 10,
    max_width = 80,
    toggle_key = '<C-h>',
    toggle_key_flip_floatwin_setting = true,
    close_timeout = 100,
    transparency = 1,
}
